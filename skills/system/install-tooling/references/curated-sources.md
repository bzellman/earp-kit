# Curated Sources

Add GitHub repos here as curated skill/agent sources. The install-tooling skill reads this file at runtime to resolve names and list available items.

## Format

Each entry: `- owner/repo (path: <path-within-repo>[, label: <display-label>])`

## Skills

- openai/skills (path: skills/.curated)
- openai/skills (path: skills/.experimental, label: experimental)

## Agents

(none configured — add owner/repo + path entries here)
